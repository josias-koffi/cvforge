# Stage 1 — Analyze

## Verdict
Pass

## Scope
- Generate a PDF export for an already-generated CV
- Use the dedicated Puppeteer/Browserless service exposed by `PUPPETEER_URL`
- Keep the rendered PDF faithful to the existing CV document template
- Strip identifying metadata by using a generic export title and no user-specific metadata fields

## Acceptance criteria
- The export PDF is generated by the dedicated service: testable through the API PDF service call and the app download route
- The render is faithful to the Puck template: testable through shared CV document markup and the same document sections
- Identifying metadata is removed: testable by the export HTML using a generic title and no author metadata

## Open questions
- None blocking

