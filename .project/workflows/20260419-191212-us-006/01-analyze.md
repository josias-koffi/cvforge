# Stage 1 — Analyze

## Scope Verdict

`US-006` is in scope for Sprint 002 and maps directly to vision sections `§2.5` and `§2.6`.

## Acceptance Criteria Interpretation

1. Codify design tokens in shared code, not only prose, so later tasks can consume them.
2. Apply the tokens to a visible base shell so the "Papier & Crayon" direction is evidenced in the UI.
3. Make the base shell mobile-first with a single-column default and larger-screen enhancement.

## Constraints

- No change to `.project/vision.md`
- No new framework or runtime dependency
- UI work must stay WCAG-conscious and mobile-first per engineering standards

## Missing Product Questions

- None blocking for this story; the vision already specifies the palette, typography direction, and mobile breakpoints.
