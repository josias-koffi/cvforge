{
  "name": "@cvforge/types",
  "private": true,
  "scripts": {
    "lint": "eslint src --ext .ts --max-warnings=0",
    "test": "vitest run --coverage",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  }
}
